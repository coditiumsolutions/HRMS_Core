@model HRMBT.Web.Models.ViewModels.EmployeeSummaryVm
@using HRMBT.Web.Infrastructure

@{
    ViewData["Title"] = "Employee Summary";
    ViewData["Module"] = "Reports";
    var selectedMonth = Model.Month;
    var selectedYear = Model.Year;
    var yearNow = DateTime.Now.Year;
    var yearMin = yearNow - 10;
    if (selectedYear.HasValue && selectedYear.Value < yearMin)
        yearMin = selectedYear.Value;
}

@section Styles {
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" crossorigin="anonymous" />
    <link rel="stylesheet" href="~/css/employee-list.css" asp-append-version="true" />
    <style>
        .emp-summary-meta {
            font-size: 0.8rem;
            color: #6c757d;
        }
        .employee-list-page .emp-summary-grid tbody td {
            color: #000;
        }
        .employee-list-page .emp-summary-grid tbody td.num {
            color: #0d6efd;
            font-weight: 700;
            font-variant-numeric: tabular-nums;
            text-align: end;
        }
        .employee-list-page .emp-summary-grid tfoot td {
            font-weight: 700;
            border-top: 2px solid #00203f;
            background: #f4f8fb;
        }
        .employee-list-page .emp-summary-grid tfoot td.num {
            color: #0a58ca;
        }
    </style>
}

<div class="employee-list-page py-3">
    <div class="container-fluid px-3 px-lg-4">
        <div class="filter-card filter-card-compact mb-3">
            <div class="card-header">
                <span><i class="bi bi-funnel"></i>Employee Summary filters</span>
            </div>
            <div class="card-body p-2 p-lg-3">
                <form asp-action="EmployeeSummary" method="get">
                    <div class="row g-2 align-items-center">
                        <div class="col-6 col-md-3">
                            <select name="month" id="month" class="form-select form-select-sm" aria-label="Month" required>
                                <option value="">— Month —</option>
                                @foreach (var monthName in PayrollMonthHelper.AllMonthNames)
                                {
                                    <option value="@monthName" selected="@(string.Equals(selectedMonth, monthName, StringComparison.OrdinalIgnoreCase))">@monthName</option>
                                }
                            </select>
                        </div>
                        <div class="col-6 col-md-2">
                            <select name="year" id="year" class="form-select form-select-sm" aria-label="Year" required>
                                <option value="">— Year —</option>
                                @for (int y = yearNow + 1; y >= yearMin; y--)
                                {
                                    <option value="@y" selected="@(selectedYear == y)">@y</option>
                                }
                            </select>
                        </div>
                        <div class="col-12 col-md-4">
                            <select name="department" id="department" class="form-select form-select-sm" aria-label="Department">
                                <option value="">All Departments</option>
                                @if (ViewBag.Departments != null)
                                {
                                    foreach (var dept in ViewBag.Departments)
                                    {
                                        var deptSelected = ViewBag.SelectedDepartment != null && ViewBag.SelectedDepartment.ToString() == dept.ToString();
                                        <option value="@dept" selected="@deptSelected">@dept</option>
                                    }
                                }
                            </select>
                        </div>
                        <div class="col-12 col-md-auto">
                            <button type="submit" class="btn btn-sm btn-filter-apply">
                                <i class="bi bi-funnel-fill me-1"></i>Apply
                            </button>
                        </div>
                    </div>
                </form>
            </div>
        </div>

        <div class="grid-card mb-3">
            <div class="card-header">
                <span><i class="bi bi-people"></i>Employee Summary</span>
            </div>
            <div class="card-body">
                @if (!Model.HasFilters)
                {
                    <div class="empty-state py-4">
                        <i class="bi bi-funnel"></i>
                        <h5 class="mb-1">Select Month and Year</h5>
                        <p class="mb-0">Choose Month and Year (optional Department), then click Apply to load employee counts and salary amounts by department.</p>
                    </div>
                }
                else if (Model.Rows.Count == 0)
                {
                    <div class="empty-state py-4">
                        <i class="bi bi-inboxes"></i>
                        <h5 class="mb-1">No departments found</h5>
                        <p class="mb-0">No employee departments match the selected filters.</p>
                    </div>
                }
                else
                {
                    <p class="emp-summary-meta px-3 pt-3 mb-2">
                        Summary for <strong>@Model.Month @Model.Year</strong>
                        @if (!string.IsNullOrWhiteSpace(Model.Department))
                        {
                            <text> · Department: <strong>@Model.Department</strong></text>
                        }
                        else
                        {
                            <text> · All departments</text>
                        }
                    </p>
                    <div class="table-wrap table-responsive">
                        <table class="employee-grid emp-summary-grid mb-0">
                            <thead>
                                <tr>
                                    <th>Department</th>
                                    <th class="text-end">Total Employees</th>
                                    <th class="text-end">Salaries Generated</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var row in Model.Rows)
                                {
                                    <tr>
                                        <td>@row.Department</td>
                                        <td class="num">@row.EmployeeCount</td>
                                        <td class="num">@row.SalariesGeneratedAmount.ToString("N2")</td>
                                    </tr>
                                }
                            </tbody>
                            <tfoot>
                                <tr>
                                    <td>Total</td>
                                    <td class="num">@Model.TotalEmployees</td>
                                    <td class="num">@Model.TotalSalariesGenerated.ToString("N2")</td>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                }
            </div>
        </div>
    </div>
</div>
